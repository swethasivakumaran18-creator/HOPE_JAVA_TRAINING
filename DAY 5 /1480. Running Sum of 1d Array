class Solution {
    public int[] runningSum(int[] nums) {
        int run[]=new int[nums.length];
        int sum=0;
        for(int i=1;i<nums.length;i++)
        {
            
            nums[i]+=nums[i-1];
           
        }
        return nums;
    }
}
